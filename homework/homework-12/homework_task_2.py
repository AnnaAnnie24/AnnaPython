#  Task 2
# Ask the user for any text, then tell whether the first & the last symbols of input text are
# equal (the same) or not
word = input("Please enter a text - ")
first_symbol = word[0]           # square brackets
last_symbol = word[-1]
# print("the first letter is " + first_symbol + "and the last letter is" + last_symbol)
print(f"The first symbol is {first_symbol}, and the last symbol is {last_symbol}")
